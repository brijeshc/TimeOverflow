import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    alignItems: "center",
  },
  clockContainer: {
    marginTop: 40,
    marginBottom: 30,
  },
  dashboardContainer: {
    alignItems: "center",
    gap: 12,
  },
});

export default styles;
